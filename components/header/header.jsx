const React = require('react');

var Component = React.createClass({
  render: function () {
    return (
      <header>
        <h1>Kabob</h1>
      </header>
    );
  }
});

module.exports = Component;
